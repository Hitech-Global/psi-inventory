# 订单预测页面·全量重算最终验收报告

> 日期：2026-07-11（本轮为「补完规则 + 全量重算 + 最终验收」收口）
> 前提：用户在【预测参数设置】补 Joypeer 的 dim 规则（印尼/Bekasi，周转=2）；在【品牌设置】将 TestBrand、测试品牌 设为停采（与 BOYA 同处理）。`sales_stats_days` 当前=60（用户此前调整）。
> 方法：全量重算 → 拉取 /api/replenishment-suggestions + /summary + /system-config → 本地复刻 getDimTurnover / 总库存池 / 目标周转命中 / 建议采购大逻辑 / 三周转 / 占比 / 顶部5项指标，逐行与真实数据比对。

## 一、全量重算结果
- 触发 `POST /api/replenishment-suggestions/generate {}` → **成功，count=396，unmatched=0**。
- **未命中规则拦截已消除**：所有 active 品牌（Redragon / Netac / Joypeer）均可命中 dim 规则；停采品牌（BOYA / TestBrand / 测试品牌）由 generate 预检 `continue` 跳过，不进 dim 预检。
- 重算前已备份 DB（`data/inventory.db.bak-full-regen-20260711-113420`）。

## 二、逐类样本对账（共 376 行明细）
| 校验项 | 结果 | 说明 |
|---|---|---|
| 总库存池 = 可用+在途+PI未发+PO未确认 | **376/376 通过** | 0 处不符 |
| 停采行 建议采购=0 | **71/71 通过** | BOYA 66 + TestBrand 3 + 测试品牌 2 |
| 停采行 动作/描述含「停止采购/停采」 | **71/71 通过** | sales_status=停采/清库存、action=停止采购优先清库存、suggestion=品牌已停采… |
| 命中周转 null（未命中） | **0 行** | 所有 active 品牌均命中 dim 规则 |
| 建议采购大逻辑（活跃行） | **305/305 通过** | target>pool→qty>0；target≤pool→qty=0；无符号反例 |
| 风险标签 × 建议动作 矛盾 | **0 矛盾** | 缺货类→补货动作；高库存/滞销→谨慎/清库存；停采 override 合理 |
| 占比（前端实时公式 onAvg/(onAvg+offAvg)） | 自洽 | 纯线上/纯线下行单渠道占比=100%，混合行和=100% |

## 三、顶部汇总指标对账（API vs 独立重算）
| 指标 | API | 独立重算 | 一致 |
|---|---|---|---|
| totalPool | 36411 | 36411 | ✅ |
| avgSalesPeriod(60天月均) | 23.06 | 23.06 | ✅ |
| overallTurnover | 4 | 4.05（四舍五入差异≤0.05，无害） | ✅ |
| needReplenish | 71 | 71 | ✅ |
| stockoutRisk | 123 | 123 | ✅ |
| highStock | 222 | 222 | ✅ |

> needReplenish 已排除 clearance（清仓）生命周期；stockoutRisk/highStock 按 risk_level / sales_group 聚合，与明细逐行重算精确一致。

## 四、口径澄清（非 bug，避免误判）
- **三周转（当前可用/在途后/下单后）前端用 period 公式实时算**：`(period分摊库存) ÷ (渠道 period 月均)`，与 summary 的 `overallTurnover=activePool/activeAvgSales`（period 口径）同源一致。
- **DB 存量字段 `current_turnover_months` 是 4m 口径历史值**（验证：≈ 可用 ÷ avg_sales_4m，375/376 吻合），前端三周转列**不读该字段展示**。两者不同是预期，不影响显示层口径一致性。
- 初验时曾把 DB 4m 字段误当"前端显示值"对比，产生 148 条"不符"，经核查为校验基准错误，非数据矛盾。

## 五、最终结论
1. **可以正式收口**：全量重算零拦截；库存池、目标周转命中、建议采购、停采规则、顶部5项汇总指标均精确一致；建议动作与风险标签自洽；无计算层/逻辑层矛盾。
2. **剩余明显问题**：无影响主线的计算或逻辑矛盾。
3. **仅列不改的待办（均为已知/设计性，非阻塞）**：
   - 【已知·设计性，验收报告D】`new_test` 生命周期行即使 `suggested_qty>0`，`suggestion` 仍显示"新品导入，不直接生成PO"，文案与"已有建议采购"略显矛盾，可后续优化文案。
   - 无其它问题。
